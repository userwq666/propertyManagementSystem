# 前端接口修复策略文档 - 方案 C

> **生成时间**: 2026-07-16  
> **项目**: 物业管理系统  
> **策略版本**: v1.0  
> **核心原则**: 保 View 层，重 API 层；后端不改，前端全量对齐；缺失接口对应功能**删除代码**（不保留、不注释）

---

## 1. 全局基础设施修改

### 1.1 请求拦截器统一加 `/api` 前缀

**文件**: `frontend/src/utils/request.js`

```javascript
// 在 request 拦截器中添加
const EXCLUDE_PREFIX = ['/auth/', '/common/upload']

request.interceptors.request.use(config => {
  if (!EXCLUDE_PREFIX.some(p => config.url.startsWith(p))) {
    config.url = '/api' + config.url
  }
  return config
})
```

**影响**: 所有 `api/*.js` 中的路径 `/system/...`、`/community/...`、`/fee/...` 等自动变为 `/api/system/...` 等，与后端 Controller `@RequestMapping` 一致。

---

### 1.2 批量删除通用工具（循环单删）

**文件**: `frontend/src/utils/batchDelete.js`（新建）

```javascript
export async function batchDelete(deleteFn, ids, options = {}) {
  const { onProgress, onError, continueOnError = true } = options
  const results = { success: [], failed: [] }
  
  for (let i = 0; i < ids.length; i++) {
    const id = ids[i]
    try {
      await deleteFn(id)
      results.success.push(id)
      onProgress?.({ current: i + 1, total: ids.length, success: true, id })
    } catch (err) {
      results.failed.push({ id, error: err })
      onProgress?.({ current: i + 1, total: ids.length, success: false, id, error: err })
      if (!continueOnError) throw err
    }
  }
  
  if (results.failed.length > 0) {
    const msg = `批量删除完成：成功 ${results.success.length}，失败 ${results.failed.length}`
    if (!continueOnError) throw new Error(msg)
    ElMessage.warning(msg)
  } else {
    ElMessage.success(`批量删除成功 ${results.success.length} 条`)
  }
  return results
}
```

**使用方式**（替换所有批量删除调用）：
```javascript
// 旧：await deleteUser(userIds)  // userIds = "1,2,3" 或 [1,2,3]
// 新：
import { batchDelete } from '@/utils/batchDelete'
await batchDelete(id => deleteUser(id), selectedRowKeys)
```

---

## 2. 模块级 API 清理 + View 代码删除清单

### 2.1 系统管理模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/system/user.js` | `getDeptTree`, `exportUser`, `importUser`, `getImportTemplate`, `getRoleList` | `views/system/user/index.vue` | 1. 部门树选择器（新增/编辑弹窗）<br>2. 导出按钮<br>3. 导入按钮+弹窗+模板下载<br>4. 角色下拉框改用 `getRoleList` 保留 |
| `api/system/menu.js` | `getMenuTreeselect`, `exportMenu` | `views/system/menu/index.vue` | 1. 菜单树选择器（角色分配权限时）<br>2. 导出按钮 |
| `api/system/role.js` | `getDeptTree`, `exportRole`, `getRoleMenuTreeselect` | `views/system/role/index.vue` | 1. 部门树选择器<br>2. 导出按钮<br>3. 菜单树选择器（权限分配） |

**参数名统一**：
- 路径参数：`userId`/`roleId`/`menuId` → `id`
- 批量删除：`userIds`/`roleIds`/`menuIds` → 使用 `batchDelete(deleteFn, ids)`

---

### 2.2 社区基础模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/community/building.js` | `changeBuildingStatus`, `exportBuilding` | `views/property/building/index.vue` | 1. 表格每行状态切换开关<br>2. 导出按钮 |
| `api/community/house.js` | `changeHouseStatus`, `exportHouse`, `bindHouseOwner`, `unbindHouseOwner` | `views/property/house/index.vue` | 1. 状态切换开关<br>2. 导出按钮<br>3. 绑定业主/解绑业主按钮+弹窗 |
| `api/community/owner.js` | `changeOwnerStatus`, `exportOwner`, `getOwnerHouseList`, `bindOwnerHouse`, `unbindOwnerHouse`, `uploadIdCard`, `getIdCardInfo`, `deleteIdCard` | `views/property/owner/index.vue` | 1. 状态切换开关<br>2. 导出按钮<br>3. 房屋列表Tab页<br>4. 绑定/解绑房屋按钮+弹窗<br>5. **身份证上传/查看/删除整块功能区** |
| `api/community/parking.js` | `changeParkingStatus`, `exportParking`, `bindParkingOwner`, `unbindParkingOwner`, `getParkingRentList` | `views/property/parking/index.vue` | 1. 状态切换开关<br>2. 导出按钮<br>3. 绑定/解绑业主按钮+弹窗<br>4. 租赁记录Tab页 |

**树形选择器降级**：
- `getBuildingTree`、`getHouseTree`、**全部删除**
- View层原用树形选择器的地方，**改用扁平下拉框**，数据源直接用分页列表 `page` 接口（`pageNum=1, pageSize=1000` 拉全量）

---

### 2.3 收费管理模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/fee/notice.js` | `sendNotice`, `getSendDetail`, `markReadStatus`, `exportNotice` | `views/fee/notice/index.vue` | 1. 发送按钮+弹窗<br>2. 发送详情弹窗<br>3. 标记已读按钮<br>4. 导出按钮 |
| `api/fee/item.js` | `changeChargeItemStatus`, `exportChargeItem`, `getChargeStandardList`, `getDictData` | `views/fee/item/index.vue` | 1. 状态切换开关<br>2. 导出按钮<br>3. 收费标准Tab页<br>4. 字典下拉数据（改用前端常量） |
| `api/fee/record.js` | `refundFeeRecord`, `exportFeeRecord`, `getFeeRecordStatistics` | `views/fee/record/index.vue` | 1. 退费按钮+弹窗<br>2. 导出按钮<br>3. 统计卡片区域 |

---

### 2.4 报修管理模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/repair/order.js` | `cancelRepairOrder`, `dispatchRepairOrder`, `processRepairOrder`, `finishRepairOrder`, `replyEvaluate`, `exportRepairOrder`, `getRepairStatistics`, `getRepairWorkerList`, `uploadImage`, `deleteImages` | `views/repair/order/index.vue`<br>`views/repair/dispatch/index.vue`<br>`views/repair/evaluate/index.vue`<br>`views/repair/worker/index.vue` | 1. **取消/派单/处理/完工/评价回复**所有状态流转按钮+弹窗<br>2. 导出按钮<br>3. 统计卡片<br>4. 维修人员选择器（派单弹窗）<br>5. **图片上传/预览/删除**整套组件 |

**保留并对齐**：
- `updateStatus(id, status, handlerId?, handleContent?)` → 对应后端 `PUT /api/repair/record/status`
- View层：仅保留“查看详情”，状态流转相关按钮**全部删除代码**

---

### 2.5 投诉建议模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/complaint/suggestion.js` | `acceptSuggestion`, `replySuggestion`, `evaluateSuggestion`, `closeSuggestion`, `exportSuggestion`, `getSuggestionStatistics`, `getHandlerList`, `uploadImage` | `views/complaint/suggestion/index.vue` | 1. **受理/回复/评价/关闭**所有状态流转按钮+弹窗<br>2. 导出按钮<br>3. 统计卡片<br>3. 处理人选择器（受理弹窗）<br>4. **图片上传/预览/删除**整套组件 |

**保留并对齐**：
- `updateStatus(id, status, handlerId?, handleContent?)` → 对应后端 `PUT /api/complaint/suggest/status`

---

### 2.6 巡检管理模块

| API文件 | 删除的导出函数 | View文件 | 删除的UI代码 |
|---------|---------------|---------|-------------|
| `api/inspection/plan.js` | `changeInspectionPlanStatus`, `exportInspectionPlan`, `getInspectionPlanStatistics`, `getInspectionPlanRecords`, `getBuildingTree`, `getEquipmentTree`, `getUserList` | `views/inspection/plan/index.vue`<br>`views/inspection/record/index.vue` | 1. 状态切换开关<br>2. 导出按钮<br>3. 统计卡片<br>4. 执行记录Tab页<br>5. 楼栋/设备/巡检人员树形选择器（新增/编辑弹窗） |

**树形选择器降级**：同社区模块，改用扁平下拉框 + 分页列表全量拉取

---

### 2.7 公告通知模块

| API文件 | 修改内容 | View文件 | 删除的UI代码 |
|---------|---------|---------|-------------|
| `api/announcement/notice.js` | **删除**：`publishNotice`, `withdrawNotice`, `toggleTopNotice`, `getNoticeStats`, `getNoticeReadStats`, `getNoticeReadTrend`, `exportNotice`<br>**路径修正**：基础路径从 `/announcement` 改为 `/announcement`（去掉多余层级），拦截器加 `/api` 后匹配后端 | `views/announcement/notice/index.vue`<br>`views/notice/announcement/index.vue` | 1. 发布/撤回/置顶按钮<br>2. 统计卡片<br>3. 阅读统计/趋势图表<br>4. 导出按钮 |

> **关键**：前端原路径 `/announcement/announcement/...` 多一层，需在 API 文件中将所有 url 改为 `/announcement/...`（单层），拦截器自动变 `/api/announcement/...`

---

### 2.8 统计模块 —— **完全重写**

**后端现有 5 个接口**（需读取 `StatisticsServiceImpl.java` 确认返回结构）：

| 接口 | 路径 | 用途 |
|------|------|------|
| 收费统计 | `GET /api/statistics/fee` | 总额/实收/未收/逾期/收缴率、按项目/按月趋势 |
| 报修统计 | `GET /api/statistics/repair` | 待确认 |
| 投诉统计 | `GET /api/statistics/complaint` | 待确认 |
| 设备统计 | `GET /api/statistics/equipment` | 待确认 |
| 业主统计 | `GET /api/statistics/owner` | 待确认 |

**新 `api/statistics/dashboard.js`**：
```javascript
import request from '@/utils/request'

export function getFeeStatistics() {
  return request({ url: '/statistics/fee', method: 'get' })
}

export function getRepairStatistics() {
  return request({ url: '/statistics/repair', method: 'get' })
}

export function getComplaintStatistics() {
  return request({ url: '/statistics/complaint', method: 'get' })
}

export function getEquipmentStatistics() {
  return request({ url: '/statistics/equipment', method: 'get' })
}

export function getOwnerStatistics() {
  return request({ url: '/statistics/owner', method: 'get' })
}
```

**View 重写** (`views/statistics/dashboard/index.vue`)：
1. 删除现有 12 个 `getXxx` 导入和调用
2. `onMounted` 中并行请求 5 个接口
3. 图表数据**前端映射转换**（如 `byMonth` → 折线图 `series.data`，`byItem` → 饼图 `series.data`）
4. ECharts `option` 配置保留，仅替换数据源

---

### 2.9 通用清理

| 内容 | 处理 |
|------|------|
| `api/fee/item.js` 中 `getDictData` | 删除，字典数据改用 `frontend/src/constants/dict.js` 维护 |
| `api/common/upload` 相关 | 不存在，不创建，View层上传组件删除 |

---

## 3. 执行顺序与验证标准

### 3.1 执行顺序（建议单人 1 天 / 双人半天）

| 顺序 | 阶段 | 预估耗时 |
|------|------|---------|
| 1 | `request.js` 拦截器 + `batchDelete.js` 新建 | 0.5h |
| 2 | 系统管理模块（3个API + 3个View） | 1h |
| 3 | 社区基础模块（4个API + 4个View） | 2h |
| 4 | 收费管理模块（3个API + 3个View） | 1.5h |
| 5 | 报修管理模块（1个API + 4个View） | 1.5h |
| 6 | 投诉建议模块（1个API + 1个View） | 1h |
| 7 | 巡检管理模块（1个API + 2个View） | 1h |
| 8 | 公告通知模块（1个API + 2个View） | 0.5h |
| 9 | 统计模块重写（1个API + 1个View） | 2h |
| **合计** | | **~10.5h** |

### 3.2 各模块完成验证清单

| 检查项 | 通过标准 |
|--------|---------|
| 列表页分页 | `pageNum`/`pageSize` 正确，表格渲染 `records`，分页组件显示 `total` |
| 新增/编辑 | 表单字段名与后端 DTO 一致，提交成功关闭弹窗、刷新列表 |
| 单个删除 | 确认弹窗后 `DELETE /api/.../id` 成功，列表更新 |
| 批量删除 | 勾选多行 → `batchDelete` 循环单删 → 全成功或聚合错误提示 |
| 下拉选择器 | 原树形选择器位置改为扁平下拉，数据来自 `page` 接口全量拉取 |
| 导出/导入/上传 | 相关按钮、弹窗、组件**代码已删除**，无残留 import、无报错 |
| 状态流转 | 报修/投诉/巡检/社区状态切换按钮**代码已删除** |
| 统计仪表盘 | 5个接口并行请求，图表渲染无错，数据映射正确 |

---

## 4. 关键决策记录（已确认）

| 决策项 | 结论 |
|--------|------|
| 后端接口改动 | **不改**（除非验证发现后端bug） |
| 缺失后端接口对应功能 | **删除代码**（不保留、不注释、不降级） |
| 树形选择器 | **全部删除**，改用扁平下拉框 + 分页列表全量数据 |
| 批量删除 | **循环单删**（`batchDelete` 工具） |
| 文件上传 | **全部砍掉**（API不创建，View删除上传组件） |
| 统计模块 | **完全重写**，仅对齐后端5个接口 |
| 请求前缀 | **拦截器统一加 `/api`**（白名单：`/auth/`, `/common/upload`） |
| 公告路径多层 | **API文件里去掉多余 `/announcement`** |

---

## 5. 风险点与应对

| 风险 | 应对 |
|------|------|
| 后端 `StatisticsServiceImpl` 返回结构与预期不符 | 先读取后端源码确认字段，再写前端映射 |
| 批量删除循环调用性能差（几十条） | 后端无批量接口无解，先跑通；必要时前端分批并发（Promise.allSettled 限制并发数 5） |
| 树形选择器改扁平下拉体验下降 | 无后端树接口无解，先保证功能可用 |
| 统计图表数据映射错 | 后端接口确认后，用 `console.log` 打印响应，逐字段映射 |
| 遗漏 import 导致构建报错 | 每模块完成后跑 `npm run build` 验证 |

---

## 6. 文件修改清单速查

### 新建/修改核心文件
- `frontend/src/utils/request.js` ← 修改拦截器
- `frontend/src/utils/batchDelete.js` ← 新建

### API层（14个文件）
- `api/system/user.js`, `menu.js`, `role.js`
- `api/community/building.js`, `house.js`, `owner.js`, `parking.js`
- `api/fee/notice.js`, `item.js`, `record.js`
- `api/repair/order.js`
- `api/complaint/suggestion.js`
- `api/inspection/plan.js`
- `api/announcement/notice.js`
- `api/statistics/dashboard.js` ← 重写

### View层（20+个文件，对应上述模块）
- `views/system/user/index.vue`, `menu/index.vue`, `role/index.vue`
- `views/property/building/index.vue`, `house/index.vue`, `owner/index.vue`, `parking/index.vue`
- `views/fee/notice/index.vue`, `item/index.vue`, `record/index.vue`
- `views/repair/order/index.vue`, `dispatch/index.vue`, `evaluate/index.vue`, `worker/index.vue`
- `views/complaint/suggestion/index.vue`
- `views/inspection/plan/index.vue`, `record/index.vue`
- `views/announcement/notice/index.vue`, `views/notice/announcement/index.vue`
- `views/statistics/dashboard/index.vue` ← 重写

---

*策略文档版本: v1.0*  
*执行前请再次确认无异议，确认后按顺序逐模块实施*